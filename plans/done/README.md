# plans/done/

Shipped phases. `mv`d from `../todo/` once the done-when checklist passes. Filenames stay as-is. All phases for an ID landed here = MVP demo-ready.

Use this folder to:
- See what's built without re-reading `mvp/`
- Diff against the root plan to spot drift
- Snapshot what shipped for a retro
